[pytest]
markers =
    release_integration: slower release-boundary coverage for installers, publication, signing, and backup or restore workflows
norecursedirs =
    data
    .git
    .venv
    node_modules
