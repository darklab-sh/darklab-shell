# darklab_shell v2.0 Release Notes

For the full list of changes included in this release, see the v2.0 section in [CHANGELOG.md](../../CHANGELOG.md).

## Highlights

- Project workspace groundwork adds the database relationship model, active project context, active project shell chrome with a clickable active-project HUD chip, shell-first `project` commands including target management and nested autocomplete, a compact Projects modal with current/archived project grouping, project deletion, a focused target editor, autosaving active-project notes, linked-run restore/unlink actions, shared explorer filters by target, run, and finding review status, run rows with per-run finding/artifact counts that jump into filtered Findings or Artifacts views, artifact availability/checksum status with scoped preview/download actions, package rows with preset/redaction/estimated-size summaries plus download/re-package/delete/manifest-preview actions, a first-pass package wizard with presets, explicit entity selection, per-run transcript selection that defaults to runs with findings, pre-build package size estimates, same-selection re-export with clearer drift messaging for missing selected entities, static `index.html`, `README.md`, selected permalink-context-backed run transcript pages, a versioned CSS/font snapshot for offline package pages, full text companions for capped transcripts, finding JSON/Markdown exports with inline metadata, target JSON/Markdown exports with inline metadata, selected label/private-annotation metadata exports, project/annotation/target note Markdown exports, artifact size/hash drift checks, project notes in package summaries, package export navigation, offline finding sorting, skipped-item reporting with pre-build unavailable-selection preview, redacted package output for manifests/HTML/Markdown/finding exports/target exports/metadata exports/note exports/transcripts/transcript text companions/filenames, active-project external-run capture controls, associated targets and inline review controls on finding rows, and clickable finding rows that restore and highlight source run lines, active-project target autocomplete, automatic external saved-run/snapshot linking with browser-owned administrative built-ins excluded from project run links, project summary aggregation, run-scoped artifact/finding/label/annotation metadata in history, restored-run JSON, workspace file listings, and permalink views, `/diag` object counts, capped draft evidence package archives with temp-file-backed download streaming, project-linked workflow promotion with truncation metadata and target-selection safeguards, run-to-run project comparison with run-label baseline selection, project finding filters, History drawer project filtering, retention warnings for expiring project-linked runs, run-scoped persisted finding capture/review with project-target matching, project target management with backend quick-add inference, entity-scoped labels and annotations, project-aware history filters, active-project preference migration, shared write-route limits, configurable project workspace quotas, async-safe app-native select option refresh for dynamic dropdowns, and first backend routes for future case-style grouping across projects, runs, snapshots, workspace files, artifacts, targets, findings, labels, annotations, and packages.
- Interactive PTY mode adds a real terminal surface for screen-oriented tools such as `mtr --interactive`, `ffuf --interactive`, and `masscan --interactive`, backed by registry-owned settings, dedicated PTY routes, Redis-friendly control routing, and a single-worker local fallback.
- The live PTY surface uses a tab-scoped xterm.js modal with fit-addon support, startup asset preloading, asset-load retry handling, theme refresh, reusable tab overlays, hidden-tab resize suppression, desktop-only mobile guardrails, and the same close/kill confirmation flow used by normal running tabs.
- PTY runs support bounded concurrency per browser session, defaulting to four active PTYs, while keeping each live terminal scoped to the tab that started it.
- Reload recovery and Status Monitor Attach restore active PTY runs from bounded ANSI terminal snapshots, preserving colors, text attributes, cursor position, and the latest visible screen when server-side terminal capture is available. Redis-backed deployments publish distributed, rate-capped snapshots so any worker can serve reattach without sticky routing.
- Completed PTY runs preserve live terminal formatting in the modal while appending a saved static transcript and exit status to the parent tab. The command registry controls transcript shaping, so `mtr` saves the final visible frame while `ffuf` and `masscan` save scrolled findings without common transient progress/status redraw lines.
- Interactive PTY mode ships with first-pass safety guardrails: explicit `input_safety` profiles, native `Ctrl+C` forwarding, rate-limited input and resize requests, UTF-8 byte-aware input trimming, workspace-CWD-aware validation before spawn, a vetted command environment, clear server-side terminal capture dependency errors, stale state cleanup, and actionable snapshot failure statuses.
- SQLite run previews are now byte-bounded with `output_preview_max_mb`, preventing huge single-line scanner output from bloating the history database while preserving larger output in full-output artifacts when enabled.
- Startup history queries now use targeted SQLite indexes so large history databases do not delay the Recent and Workflows rail menus.
- History restore now distinguishes runs by run identity, so restoring a command no longer jumps to an unrelated existing tab with the same command text.
- The command composer now blocks macOS double-space period substitution from changing terminal input.
- Recent value capture now preserves complete IPv4 addresses entered in domain-style argument slots.
